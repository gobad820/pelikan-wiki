---
title: 강의 노트 - 2026-02-10
type: lecture
tags: [n-queen, backtracking, pruning]
related:
  - "[[Backtracking]]"
created: 2026-04-06
updated: 2026-04-07
sources: [day0210]
---

## 오늘 배운 패턴/개념
- [[Backtracking]] - N-Queen: 대각선 가지치기

## 핵심 코드
```java
static boolean[] col, slash, bslash;
// slash[c + row]: 우상향 대각선 (같은 값 = 같은 대각선)
// bslash[c - row + n]: 우하향 대각선

void setQueen(int row) {
    if (row == n + 1) { totalCnt++; return; }
    for (int c = 1; c <= n; c++) {
        if (col[c] || slash[c + row] || bslash[c - row + n]) continue;
        col[c] = slash[c + row] = bslash[c - row + n] = true;
        setQueen(row + 1);
        col[c] = slash[c + row] = bslash[c - row + n] = false;
    }
}
// 배열 크기: col[n+1], slash[2n+1], bslash[2n]
```

## 문제 풀이
- NQueenTest: N×N 체스판에 N개 퀸 배치 경우의 수

## 오늘의 인사이트
- 대각선 인덱스 공식: `c + row` (우상향), `c - row + n` (우하향)
- 행 기준으로 재귀 → 자동으로 같은 행 중복 배치 방지
- 열/대각선만 3개 배열로 O(1) 충돌 체크 가능

## 다음에 복습할 것
- N-Queen 비트마스크 최적화 버전
